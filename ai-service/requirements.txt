# Web server
fastapi>=0.115
uvicorn[standard]>=0.30
python-dotenv>=1.0
python-multipart>=0.0.9
pydantic>=2.6

# Image / numerics
Pillow>=10
numpy>=1.26

# Model loading. NOTE: torch/torchvision are installed separately from the CUDA
# 12.8 wheel index by install.ps1 / install.sh (RTX 50-series / Blackwell need cu128).
torch>=2.7
# diffusers main has Flux2KleinPipeline + Flux2Transformer2DModel.from_single_file (GGUF).
diffusers @ git+https://github.com/huggingface/diffusers.git
transformers>=4.57.1
accelerate>=1.10
safetensors>=0.4
sentencepiece
protobuf
# GGUF transformer loading (Flux2Transformer2DModel.from_single_file).
gguf>=0.10.0
# 4-bit (nf4) quantisation of the Qwen3 text encoder.
bitsandbytes>=0.45
# AI upscaler (Real-ESRGAN-class models) for the vectoriser.
spandrel>=0.4

# HF hub for downloads
huggingface-hub>=0.25
